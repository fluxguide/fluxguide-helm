apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testingchart.fullname" . }}-test-connection"
  labels:
    {{- include "testingchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testingchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
